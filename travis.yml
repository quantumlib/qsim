language: cpp

os:
  - linux

services:
  - docker

before_script:
  - docker-compose build

script:
  - docker run -ti --rm qsim-cxx-tests:latest
  - docker run -ti --rm qsim-py-tests:latest
  - docker run -ti --rm qsim:latest -c /qsim/circuits/circuit_q24
